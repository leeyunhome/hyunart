import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
    // plugins: [react()], // React plugin disabled as this appears to be a vanilla JS project
    base: '/hyunart/',
})
